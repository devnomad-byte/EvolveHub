# ==============================================================
# EvolveHub AI Platform - Development Profile
# ==============================================================
server:
  port: 8082

spring:
  # ----- 多数据源配置 -----
  datasource:
    # 主数据源：AI 库（知识库、文档、切片等）
    ai:
      jdbc-url: jdbc:postgresql://106.15.231.177:${DB_PORT:5432}/evolve_ai
      username: admin
      password: a774461674
      driver-class-name: org.postgresql.Driver
    # 从数据源：Auth 库（读取公共表：部门、用户等）
    auth:
      jdbc-url: jdbc:postgresql://106.15.231.177:${DB_PORT:5432}/evolve_auth
      username: admin
      password: a774461674
      driver-class-name: org.postgresql.Driver

  # ----- Redis (database 0, shared with auth for Sa-Token session) -----
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      database: 0

# ----- Logging (development: show SQL) -----
logging:
  level:
    org.evolve: DEBUG
    com.baomidou.mybatisplus: DEBUG
